# Xero Integration
XERO_CLIENT_ID=****
XERO_CLIENT_SECRET=****
XERO_REDIRECT_URI=https://your-domain.com/api/xero/callback
XERO_WEBHOOK_KEY=****

# Token Encryption Key (Generate with: openssl rand -hex 64)
# Note: Ensure this is a hex string representing at least 32 bytes (64 hex chars)
# This is the current active encryption key (version 1)
TOKEN_ENC_KEY_HEX=****

# Key Rotation Support:
# When rotating encryption keys, keep old keys available for decryption:
# 1. Generate a new key: openssl rand -hex 64
# 2. Set TOKEN_ENC_KEY_V2_HEX with the new key
# 3. Update CURRENT_KEY_VERSION in lib/utils/encryption.ts to 2
# 4. Keep TOKEN_ENC_KEY_HEX for decrypting old tokens
# 5. Run re-encryption migration script to update existing tokens
# Example for old keys:
# TOKEN_ENC_KEY_V0_HEX=****  # Previous key (if needed)
